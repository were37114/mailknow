# Phase 0 W1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** PGLite + GBrain Schema + IMAP + Parser + Electron 脚手架

**Tech Stack:** Python 3.11+, PGLite, pgvector, FastAPI, Electron, React, TypeScript

---

## Task 1: PGLite + GBrain Schema（D1）

**Files:** `pyproject.toml`, `src/db/schema.sql`, `src/db/pgpool.py`, `tests/unit/test_pgpool.py`

- [ ] **创建 pyproject.toml**（依赖：pglite, psycopg, pgvector, fastapi, pytest）

- [ ] **创建 schema.sql**（pages/links/entities 三表 + pgvector + 索引）

- [ ] **创建 pgpool.py**（PGLitePool 类：start/stop/get_connection/init_schema）

- [ ] **写测试**（启动、连接、建表、插入）

- [ ] **运行测试验证**

```bash
cd /Users/yc-m4/WorkBuddy/Claw/mailknow
/Users/yc-m4/.workbuddy/binaries/python/envs/default/bin/pip install -e ".[dev]"
/Users/yc-m4/.workbuddy/binaries/python/envs/default/bin/python -m pytest tests/unit/test_pgpool.py -v
```

---

## Task 2: PGLite 持久化测试（D2）

**Files:** `tests/integration/test_pglite_persistence.py`

- [ ] **写持久化测试**（重启后数据完整、500封邮件持久化）

- [ ] **运行测试验证**

```bash
/Users/yc-m4/.workbuddy/binaries/python/envs/default/bin/python -m pytest tests/integration/test_pglite_persistence.py -v
```

---

## Task 3: IMAP 连接器（D3）

**Files:** `src/sync/models.py`, `src/sync/imap_sync.py`, `tests/unit/test_imap_sync.py`

- [ ] **创建 models.py**（Email, EmailAddress, EmailFlag, IMAPFolder）

- [ ] **创建 imap_sync.py**（IMAPConnector: connect/list_folders/fetch_messages）

- [ ] **写测试**（连接、列表、拉取）

---

## Task 4: 邮件解析（D4）

**Files:** `src/sync/parser.py`, `tests/unit/test_parser.py`

- [ ] **创建 parser.py**（编码检测、嵌套解析、附件提取）

- [ ] **写测试**（GBK/UTF-8、multipart、附件）

---

## Task 5: Electron 脚手架（D5）

**Files:** `client/package.json`, `client/main/index.ts`, `client/renderer/App.tsx`

- [ ] **创建 package.json**（electron, react, typescript, vite）

- [ ] **创建主进程**（BrowserWindow + IPC）

- [ ] **创建渲染进程**（React Hello World）

- [ ] **启动验证**

```bash
cd /Users/yc-m4/WorkBuddy/Claw/mailknow/client
npm install
npm run dev
```

---

## 验收标准

| 任务 | 验收 |
|------|------|
| D1 | PGLite启动<5s，pages/links/entities表创建 |
| D2 | 500封邮件写入，重启后数据完整 |
| D3 | 能连接163邮箱，拉取邮件列表 |
| D4 | 支持GBK/UTF-8，正确解析嵌套邮件 |
| D5 | Electron+React启动，Hello World渲染 |
